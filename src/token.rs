#[derive(Debug, Clone, PartialEq)]
pub enum Token {
    // Keyword
	AND,
	BEGIN,
	CASE,
	DO,
	ELSE,
	END,
	FOR,
	IF,
	IN,
	INTEGER,
	IS,
	NEW,
	NOT,
	OBJECT,
	OR,
	PRINT,
	PROCEDURE,
	READ,
	RETURN,
	THEN,
	// symbols
	ADD,
	SUBTRACT,
	MULTIPLY,
	DIVIDE,
	ASSIGN,
	EQUAL,
	LESS,
	COLON,
	SEMICOLON,
	PERIOD,
	COMMA,
	LPAREN,
	RPAREN,
	LSQUARE,
	RSQUARE,
	LCURL,
	RCURL,
	// special
	CONST(u16),
	ID(String),
	STRING(String),
	EOS,
	ERROR(String)
}